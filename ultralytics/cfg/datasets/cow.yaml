# Ultralytics YOLO 🚀, GPL-3.0 license
# head_shoulder dataset
# Example usage: yolo train data=head_shoulder.yaml
# parent
# ├── ultralytics
# └── datasets
#     └── head_shoulder  ← downloads here (7 MB)


# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
#1)
#path: ../datasets/head_shoulder_dataset  # dataset root dir
#train: images/train  # train images (relative to 'path') 128 images
#val: images/train  # val images (relative to 'path') 128 images

#2)

path: /home/workspace/Feng/dataset_cow  # dataset root dir
train: train.txt  # train images (relative to 'path') 128 images
val: test.txt  # val images (relative to 'path') 128 images
test:  # test images (optional)

# Classes
names:
  0: cow

